# Пошаговый расчет коэффициента кормления для бассейна 9

## 📊 Исходные данные

### ШАГ 1: Получение данных активной сессии
**Источник**: Таблица `sessions`, запрос: `WHERE pool_id = 9 AND is_completed = 0`

| Параметр | Значение | Источник |
|----------|----------|----------|
| ID сессии | 8 | `sessions.id` |
| Название | Средняя | `sessions.name` |
| Бассейн | 9 | `sessions.pool_id` |
| Корм ID | 4 (F3) | `sessions.feed_id` |
| Стратегия | normal | `sessions.feeding_strategy` |
| Кормлений в день | 3 | `sessions.daily_feedings` |
| Начальная масса | 1741.00 кг | `sessions.start_mass` |
| Начальное количество | 1992 шт | `sessions.start_fish_count` |
| **Начальный средний вес** | **0.874 кг (874 г)** | `start_mass / start_fish_count` |

---

### ШАГ 2: Получение температуры
**Источник**: Таблица `measurements`, последнее измерение для `pool_id = 9`

| Параметр | Значение | Источник |
|----------|----------|----------|
| Температура | **12.20°C** | `measurements.temperature` |
| Дата измерения | 2025-12-17 17:03:00 | `measurements.measured_at` |

---

### ШАГ 3: Определение среднего веса рыбы
**Источник**: Таблица `weighings` или начальные данные сессии

**Алгоритм выбора:**

1. **Поиск навесок с session_id = 8**
   ```sql
   SELECT * FROM weighings 
   WHERE session_id = 8 
   ORDER BY recorded_at DESC LIMIT 1
   ```
   **Результат**: ❌ НЕ НАЙДЕНО

2. ~~Старый метод (pool_id + start_date)~~ 
   - **ОТКЛЮЧЕН** - может найти навески от других сессий
   - Пример: навеска ID 1 (session_id=5, вес 46 г) относится к старой сессии и НЕ должна использоваться

3. **Использование начальных данных сессии**
   ```php
   avg_weight = start_mass / start_fish_count
   avg_weight = 1741.00 / 1992 = 0.874 кг = 874 г
   ```
   **Результат**: ✅ **874 г** (используется)

**Итоговый вес для расчета**: **874 г**

---

### ШАГ 4: Расчет текущей биомассы
**Источник**: `sessions.start_mass` минус суммы `harvests` и `mortality` для `session_id = 8`

**Расчет:**
```
current_weight = start_mass - SUM(harvests.weight) - SUM(mortality.weight)
current_weight = 1741.00 - 7025.42 - 291.52 = -5575.94 кг
current_weight = max(0, -5575.94) = 0 кг
```

| Параметр | Значение |
|----------|----------|
| Начальная масса | 1741.00 кг |
| Отобрано (harvests) | 7025.42 кг |
| Падеж (mortality) | 291.52 кг |
| **Текущая биомасса** | **0 кг** |

⚠️ **ПРОБЛЕМА**: Отобрано больше, чем было начальной массы - ошибка в данных!

---

### ШАГ 5: Получение таблицы кормления
**Источник**: `feeds.formula_normal` для корма ID 4 (F3)

**Структура таблицы:**
- **Единица измерения**: "kg feed per day per 100 kg fish"
- **Температуры**: 6, 8, 10, 12, 14, 16, 18°C
- **Диапазоны веса**: 
  - 5-10 г
  - 10-40 г
  - 40-100 г
  - 100-200 г
  - 200-400 г
  - 300-400 г
  - 400-500 г
  - 500-600 г
  - 600-800 г
  - **800-1000 г** ← используется для веса 874 г
  - 1000-2000 г
  - >2000 г

---

### ШАГ 6: Поиск диапазона веса
**Алгоритм**: Перебираем диапазоны по порядку, находим первый, где:
```
weight >= min && (max === null || weight < max)
```

**Для веса 874 г:**
```
874 >= 5 && 874 < 10?   → НЕТ
874 >= 10 && 874 < 40?  → НЕТ
874 >= 40 && 874 < 100? → НЕТ
...
874 >= 800 && 874 < 1000? → ✅ ДА! ВЫБРАН
```

**Результат**: Диапазон **"800-1000 (6.0 мм)"**

---

### ШАГ 7: Поиск диапазона температуры
**Алгоритм**: Находим два соседних значения температуры, между которыми находится текущая

**Для температуры 12.20°C:**
```
12.20 <= 6?  → НЕТ
12.20 <= 8?  → НЕТ
12.20 <= 10? → НЕТ
12.20 <= 12? → НЕТ (12.20 > 12)
12.20 <= 14? → ДА, но 12.20 > 12
```

**Результат**: 
- Температура находится **между** 12°C и 14°C
- Нужна **линейная интерполяция**
- Температура нижняя: **12°C**
- Температура верхняя: **14°C**

---

### ШАГ 8: Получение коэффициентов из таблицы
**Источник**: Секция `values` в таблице кормления, для диапазона "800-1000"

**Значения в таблице:**
| Температура | Коэффициент для "800-1000" |
|-------------|---------------------------|
| 12°C | **1.00** |
| 14°C | **1.20** |

---

### ШАГ 9: Расчет коэффициента с учетом стратегии
**Стратегия**: `normal` (Норма)

**Формула линейной интерполяции:**
```
coeff = coeffLower + (coeffUpper - coeffLower) * (temp - tempLower) / (tempUpper - tempLower)
```

**Подстановка значений:**
```
coeff = 1.00 + (1.20 - 1.00) * (12.20 - 12) / (14 - 12)
coeff = 1.00 + 0.20 * 0.20 / 2
coeff = 1.00 + 0.20 * 0.10
coeff = 1.00 + 0.02
coeff = 1.02
```

**Промежуточные значения:**
- `coeffLower` = 1.00 (коэффициент при 12°C)
- `coeffUpper` = 1.20 (коэффициент при 14°C)
- `temp` = 12.20°C (текущая температура)
- `tempLower` = 12°C (нижняя граница температуры)
- `tempUpper` = 14°C (верхняя граница температуры)
- `ratio` = (12.20 - 12) / (14 - 12) = 0.20 / 2 = **0.10** (позиция между температурами, 10%)

**Результат**: **1.02**

---

### ШАГ 10: Расчет количества корма
**Формулы:**
```
ratio_per_100kg = 1.02
recommended_per_day = (1.02 / 100) * current_biomass
per_feeding = recommended_per_day / daily_feedings
```

**Расчет:**
```
recommended_per_day = (1.02 / 100) * 0 = 0 кг
per_feeding = 0 / 3 = 0 кг
```

**Результат**: 
- Коэффициент кормления: **1.02**
- На одно кормление: 0 кг (биомасса = 0)

---

## 📋 Итоговая таблица промежуточных значений

| Шаг | Параметр | Значение | Источник |
|-----|----------|----------|----------|
| 1 | ID сессии | 8 | `sessions.id` |
| 1 | Корм ID | 4 | `sessions.feed_id` |
| 1 | Стратегия | normal | `sessions.feeding_strategy` |
| 1 | Начальный вес | 874 г | `sessions.start_mass / start_fish_count` |
| 2 | Температура | 12.20°C | `measurements.temperature` (последнее) |
| 3 | Средний вес | 874 г | Из начальных данных (навесок нет) |
| 4 | Биомасса | 0 кг | `start_mass - harvests - mortality` |
| 6 | Диапазон веса | "800-1000" | Поиск в таблице кормления |
| 7 | Темп. нижняя | 12°C | Поиск в таблице кормления |
| 7 | Темп. верхняя | 14°C | Поиск в таблице кормления |
| 8 | Коэфф. при 12°C | 1.00 | `feeds.formula_normal` → values[12]["800-1000"] |
| 8 | Коэфф. при 14°C | 1.20 | `feeds.formula_normal` → values[14]["800-1000"] |
| 9 | Позиция (ratio) | 0.10 (10%) | (12.20 - 12) / (14 - 12) |
| 9 | **ИТОГОВЫЙ КОЭФФИЦИЕНТ** | **1.02** | 1.00 + 0.20 * 0.10 |

---

## ❓ Почему может показываться 1.51?

Если в интерфейсе отображается **1.51** вместо **1.02**, возможные причины:

### 1. Использование старой навески (46 г) - ИСПРАВЛЕНО
- Диапазон: "40-100"
- При 12°C: 2.5, при 14°C: 2.5
- Результат: 2.5 ❌ (не 1.51)

### 2. Использование диапазона "400-500"
- При 12°C: 1.20, при 14°C: 1.50
- Интерполяция: 1.20 + (1.50 - 1.20) * 0.10 = 1.23 ❌ (не 1.51)
- При стратегии "growth": max(1.20, 1.50) = 1.50 ⚠️ (близко, но не 1.51)

### 3. Использование значения при 14°C для диапазона "400-500"
- Значение: 1.50
- Возможно округление: 1.51 ⚠️

### 4. Кеш браузера/PHP
- Старое значение закешировано
- **Решение**: Обновить страницу (Ctrl+F5) или очистить кеш

---

## ✅ Правильный расчет (после исправления)

**Входные данные:**
- Вес: **874 г** (начальный вес сессии, НЕ навеска 46 г)
- Температура: **12.20°C**
- Стратегия: **normal**

**Расчет:**
1. Диапазон веса: **"800-1000"** (для 874 г)
2. Температурный диапазон: **12°C - 14°C**
3. Коэффициенты: **1.00** (12°C) и **1.20** (14°C)
4. Интерполяция: 1.00 + (1.20 - 1.00) * 0.10 = **1.02**

**Ожидаемый результат**: **1.02**

---

## 🔧 Что было исправлено

1. **Убрана логика использования навесок от других сессий**
   - Раньше: `findLatestSince` мог найти навеску от старой сессии (46 г)
   - Теперь: Используются только навески с `session_id` текущей сессии

2. **Добавлена отладка**
   - Логи записываются в `storage/feeding_debug.log`
   - Можно проверить, какие значения реально используются

---

## 📝 Проверка

Запустите скрипт для проверки:
```bash
docker exec fisherp_web php /var/www/html/debug_feeding_step_by_step.php
```

Проверьте логи после обновления страницы:
```bash
docker exec fisherp_web tail -20 /var/www/html/storage/feeding_debug.log
```

